# dependencies
node_modules

# next.js
.next
out
next-env.d.ts

# production / deploy
.vercel
build

# secrets — NEVER commit these
.env
.env.local
.env.*.local
.env.development
.env.production

# Claude Code local config (may contain machine-specific keys/paths)
.claude/settings.local.json

# local brief cache (generated at runtime)
.brief-cache

# misc
.DS_Store
*.log
